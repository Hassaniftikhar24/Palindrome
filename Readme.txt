Task: Building a python function which checks if the string is a palindrome or not.
Palindrome: A palindrome is a word or phrase that reads the same forwards as it does backwards e.g level , Go hand a salami - I'm a lasagna hog. 
Input: string which is to be evaluated
Output: Boolean output showing True or False
Note: Function should only consider letters (A-Z)
    : Ignore upper and lower case
